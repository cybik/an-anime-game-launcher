{
    "conventionalCommits.scopes": [
        "tracing",
        "core",
        "ui",
        "i18n"
    ],
    "editor.tabCompletion": "on",
    "diffEditor.codeLens": true,
    "docker.commands.run": "${containerCommand} run --rm -t -d -v ${workspaceFolder}:/Depot ${exposedPorts} ${tag}"
}